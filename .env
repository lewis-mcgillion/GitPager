# Committed, non-secret defaults needed by the Prisma CLI (migrate/generate/seed).
# Real secrets (SESSION_SECRET, OIDC_CLIENT_SECRET) must go in .env.local — NEVER here.
DATABASE_URL="file:./dev.db"
